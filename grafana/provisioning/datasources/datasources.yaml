apiVersion: 1
datasources:
  - name: "Metric - Prometheus"
    type: prometheus
    uid: prometheus
    access: proxy
    url: http://prometheus:9090
    access: proxy
    isDefault: true
    editable: false
